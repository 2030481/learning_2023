#include <stdio.h>
#include <stdlib.h>

int main() {
    char str[] = "12345"; // Example string

    int num = atoi(str);

    printf("String: %s\n", str);
    printf("Integer: %d\n", num);

    return 0;
}